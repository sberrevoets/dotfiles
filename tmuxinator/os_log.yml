name: os_log
root: ~/Developer/Lyft-iOS/util

windows:
  - editor:
      layout: main-vertical
      panes:
        - clear && ./os_log -u Lyft
        - clear && ./os_log -u LyftNetworking -c Error
        - clear && ./os_log -c Verbose
